% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interventions.R
\name{set_intervention_ach}
\alias{set_intervention_ach}
\title{Install an intervention into the parameters list}
\usage{
set_intervention_ach(
  parameters_list,
  setting,
  coverage_target,
  coverage_type,
  timestep,
  intervention
)
}
\arguments{
\item{parameters_list}{A list of model parameters as generated by
\code{\link[=get_parameters]{get_parameters()}}.}

\item{setting}{Character. One of \code{"workplace"}, \code{"school"}, \code{"leisure"},
\code{"household"}, or \code{"joint"}.}

\item{coverage_target}{Character. What the coverage fraction applies to —
either \code{"individuals"} (count of people) or \code{"square_footage"}
(size-weighted, using \verb{size_per_individual_<setting>}).}

\item{coverage_type}{Character. How locations are selected for coverage —
either \code{"random"} (uniform sampling until the coverage budget is met) or
\code{"targeted_riskiness"} (locations ranked in decreasing order of riskiness).}

\item{timestep}{Numeric. First simulation timestep at which the
intervention's efficacy is applied in the FOI calculation.}

\item{intervention}{One intervention object as returned by \code{\link[=make_intervention]{make_intervention()}}.}
}
\value{
The input \code{parameters_list} with the \verb{intervention_<setting>_*}
slots populated. For \code{setting = "joint"}, the \code{intervention_joint_coverage}
slot is additionally populated from the intervention object's \code{coverage}
field.
}
\description{
Stores a single intervention object (as produced by \code{\link[=make_intervention]{make_intervention()}})
into the parameters list under the chosen \code{setting}. The intervention is
not yet allocated to specific locations — that happens later via
\code{\link[=generate_intervention_switches]{generate_intervention_switches()}} when \code{\link[=create_variables]{create_variables()}} runs.

Setting "joint" pools workplace + school + leisure into a single coverage
budget; household is intentionally excluded from joint deployment. The
four per-setting options ("workplace", "school", "leisure", "household")
install the intervention independently into one setting.

Currently single-intervention only — passing more than one intervention
will error. Multi-intervention support lives on a separate branch.
}
\seealso{
Other intervention: 
\code{\link{ach_to_efficacy}()},
\code{\link{calculate_efficacy_from_ach}()},
\code{\link{efficacy_to_delta}()},
\code{\link{generate_intervention_switches}()},
\code{\link{generate_joint_intervention_switches}()},
\code{\link{generate_setting_intervention_switches}()},
\code{\link{make_intervention}()},
\code{\link{uv_to_delta}()}
}
\concept{intervention}
