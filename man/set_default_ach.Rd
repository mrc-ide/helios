% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setting_specific_ach.R
\name{set_default_ach}
\alias{set_default_ach}
\title{Set a uniform default ACH for a setting type}
\usage{
set_default_ach(parameters_list, setting, ach)
}
\arguments{
\item{parameters_list}{A list of model parameters as generated by
\code{\link[=get_parameters]{get_parameters()}}.}

\item{setting}{Character. One of \code{"workplace"}, \code{"school"}, \code{"leisure"},
or \code{"household"}.}

\item{ach}{Numeric. The uniform ACH value assigned to every location in
the setting (1/hour). Must be non-negative.}
}
\value{
The input \code{parameters_list} with \verb{default_ach_<setting>} set to
\code{ach}.
}
\description{
Configures a single setting so that every location in it has the same
baseline ACH. This is the "uniform" alternative to
\code{\link[=set_setting_specific_ach]{set_setting_specific_ach()}}, which draws per-location ACH values from a
distribution.

Sets the package parameter \verb{default_ach_<setting>} to \code{ach}. Because the
setting-specific switch stays FALSE, the model treats this setting as
"no per-location variation" — every location in the setting gets ACH equal
to \code{ach}.

For every setting used in a simulation, the user must call either
\code{\link[=set_setting_specific_ach]{set_setting_specific_ach()}} (distribution) or \code{\link[=set_default_ach]{set_default_ach()}}
(uniform); otherwise \code{create_variables()} will error.
}
\seealso{
Other setting-specific ACH: 
\code{\link{convert_ach_to_riskiness}()},
\code{\link{generate_setting_specific_ach}()},
\code{\link{set_setting_specific_ach}()}
}
\concept{setting-specific ACH}
