% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/setting_specific_ach.R
\name{convert_ach_to_riskiness}
\alias{convert_ach_to_riskiness}
\title{Convert per-location ACH values to per-location riskiness}
\usage{
convert_ach_to_riskiness(ach_values, parameters_list, setting)
}
\arguments{
\item{ach_values}{Numeric vector of per-location baseline ACH values
(1/hour), as returned by \code{\link[=generate_setting_specific_ach]{generate_setting_specific_ach()}}.}

\item{parameters_list}{A list of model parameters as generated by
\code{\link[=get_parameters]{get_parameters()}}. The Wells-Riley parameters
(\code{wells_riley_emission_rate}, \code{wells_riley_decay_rate},
\code{wells_riley_infection_prob_per_ffu},
\code{wells_riley_respiratory_rate_factor}, \code{wells_riley_time_in_room}) and
the setting volume (\verb{volume_per_person_<setting>}) are read from here.}

\item{setting}{Character. One of \code{"workplace"}, \code{"school"}, \code{"leisure"},
or \code{"household"}. Used to select the appropriate \verb{volume_per_person_*}.}
}
\value{
Numeric vector of per-location riskiness values, same length as
\code{ach_values}, anchored so the median location has riskiness ~ 1.
}
\description{
Applies the Wells-Riley framework to convert each location's baseline ACH
into a relative riskiness value (a per-location multiplier on the FOI).

For each location \code{i}:
\deqn{p_{inf}(i) = 1 - \exp(-r \cdot C_{ss}(i) \cdot RR_{tv} \cdot t)}
\deqn{C_{ss}(i) = (I \cdot \pi) / (\alpha_i \cdot V), \quad \alpha_i = ACH_i + k_D}

Riskiness is then normalised so a typical location has value 1:
\deqn{riskiness_i = p_{inf}(i) / p_{inf}(ACH_{median})}

Because the Wells-Riley exponent is small for our parameter values,
\verb{1 - exp(-x) \\approx x}, and the cancellation simplifies riskiness to
approximately \code{(ACH_ref + kD) / (ACH_i + kD)} — i.e., it depends only on
ACH. The constants \code{I}, \code{pi}, \code{RR_tv}, \code{t}, and \code{V} cancel.

Called once per setting during \code{\link[=create_variables]{create_variables()}} init, immediately
after \code{\link[=generate_setting_specific_ach]{generate_setting_specific_ach()}}. The result is stored at
\verb{<setting>_specific_riskiness} and used by \code{processes.R} to scale the
per-setting FOI.
}
\seealso{
Other setting-specific ACH: 
\code{\link{generate_setting_specific_ach}()},
\code{\link{set_default_ach}()},
\code{\link{set_setting_specific_ach}()}
}
\concept{setting-specific ACH}
