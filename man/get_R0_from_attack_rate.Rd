% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_varying_transmission_calibration.R
\name{get_R0_from_attack_rate}
\alias{get_R0_from_attack_rate}
\title{Back-solve R0 from a final attack rate via the final-size relation}
\usage{
get_R0_from_attack_rate(attack_rate)
}
\arguments{
\item{attack_rate}{Numeric scalar, must be in (0, 1). \code{attack_rate = 0}
gives 0/0, \code{attack_rate >= 1} gives log of <= 0, both undefined.}
}
\value{
Numeric scalar R0, or \code{NA} if \code{attack_rate} is outside (0, 1)
}
\description{
Helper for \code{\link[=run_beta_sweep]{run_beta_sweep()}}. Solves \code{AR = 1 - exp(-R0 * AR)} explicitly
for R0: rearranging gives \code{R0 = -log(1 - AR) / AR} (R0 appears once after
taking logs, so no numerical root-finder is needed).
}
