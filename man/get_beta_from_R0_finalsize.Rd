% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_varying_transmission_calibration.R
\name{get_beta_from_R0_finalsize}
\alias{get_beta_from_R0_finalsize}
\title{Map a target R0 (or Rt(t) series) to beta_community via final-size data}
\usage{
get_beta_from_R0_finalsize(
  target,
  sweep_db,
  linear_threshold = 1.2,
  min_linear_points = 3L
)
}
\arguments{
\item{target}{Numeric scalar (a single target R0) or numeric vector (an
Rt(t) time series). Values > 1 are interpolated against the final-size
table; values <= 1 use a linear extrapolation fit to swept points near
the epidemic threshold (see \code{linear_threshold} and \code{min_linear_points}).}

\item{sweep_db}{Either the list object returned by \code{run_beta_sweep()}, or
a file path (character string) to an \code{.rds} file saved by it.}

\item{linear_threshold}{R0 value defining the upper boundary of the
near-threshold region used to fit the linear extrapolation. All swept
points with R0 <= \code{linear_threshold} are used if there are at least
\code{min_linear_points} of them; otherwise the \code{min_linear_points} swept
points closest to R0 = 1 are used instead. Default 1.2.}

\item{min_linear_points}{Minimum number of points required in the
near-threshold region before falling back to the closest-points approach.
Default 3.}
}
\value{
A data.frame with one row per element of \code{target}, and columns:
\itemize{
\item \code{index}: position within \code{target} (useful for an Rt(t) series)
\item \code{target_R0}: the R0 or Rt value supplied
\item \code{beta_community}: the corresponding beta_community value
\item \code{source}: \code{"sweep_data"} if \code{target_R0 > 1} (interpolated directly
from the sweep), or \code{"linear_extrapolation"} if \code{target_R0 <= 1}
}
}
\description{
Prints a summary of the transmission fractions used in the underlying
sweep, the results table, and a plot of the mapped beta_community
values across the target sequence, then returns that same table.
}
\details{
Note: this method assumes a fully susceptible population. When applied to
an Rt(t) series, each value is looked up independently against the static
beta-R0 relationship and does not account for time-varying susceptible
depletion interacting with a changing Rt.
}
