% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parameters.R
\name{get_parameters}
\alias{get_parameters}
\title{Establish the list of model parameters}
\usage{
get_parameters(overrides = list(), archetype = "none")
}
\arguments{
\item{overrides}{A named list of parameters values to be used instead of the defaults.
These parameters are:
\itemize{
\item \code{human_population}: the number of humans to include in the model
\item \code{initial_proportion_child}: proportion of population initially in the 'child' age class
\item \code{initial_proportion_adult}: proportion of population initially in the 'adult' age class
\item \code{initial_proportion_elderly}: proportion of population initially in the 'elderly' age class
\item \code{number_initial_S}: number of humans initially Susceptible (state = S)
\item \code{number_initial_E}: number of humans initially Exposed (state = E)
\item \code{number_initial_I}: number of humans initially Infectious (state = I)
\item \code{number_initial_R}: number of humans initially Recovered (state = R)
\item \code{seed}: a seed to run the simulation with
\item \code{mean_household_size}: TBD
\item \code{workplace_prop_max}: maximum size of a workplace as a proportion of total adult population size
\item \code{workplace_a}: the a parameter for the Zipf-like distribution on workplace size
\item \code{workplace_c}: the c parameter for the Zipf-like distribution on workplace size
\item \code{school_prop_max}: maximum size of a school as a proporiton of total child population size
\item \code{school_meanlog}: the meanlog parameter for the log-normal distribution on school size
\item \code{school_sdlog}: the sdlog parameter for the log-normal distribution on school size
\item \code{school_student_staff_ratio}: the number of students to each adult staff member
\item \code{leisure_mean_number_settings}: TBD
\item \code{leisure_mean_size}: TBD
\item \code{leisure_overdispersion_size}: TBD
\item \code{leisure_prop_max}: TBD
\item \code{duration_exposed}: TBD
\item \code{duration_infectious}: TBD
\item \code{prob_inf_external}: The probability a susceptible individual is infected from an external source
\item \code{beta_household}: The transmission rate for household contacts, used to compute
the per-timestep force of infection in household settings. A numeric scalar
when \code{time_varying_transmission_on} is \code{FALSE}, or a numeric vector of length
\code{simulation_time} (one value per simulated calendar day) when it is \code{TRUE}.
\item \code{beta_workplace}: The transmission rate for workplace contacts, used to compute
the per-timestep force of infection in workplace settings. A numeric scalar
when \code{time_varying_transmission_on} is \code{FALSE}, or a numeric vector of length
\code{simulation_time} (one value per simulated calendar day) when it is \code{TRUE}.
\item \code{beta_school}: The transmission rate for school contacts, used to compute
the per-timestep force of infection in school settings. A numeric scalar
when \code{time_varying_transmission_on} is \code{FALSE}, or a numeric vector of length
\code{simulation_time} (one value per simulated calendar day) when it is \code{TRUE}.
\item \code{beta_leisure}: The transmission rate for leisure contacts, used to compute
the per-timestep force of infection in leisure settings. A numeric scalar
when \code{time_varying_transmission_on} is \code{FALSE}, or a numeric vector of length
\code{simulation_time} (one value per simulated calendar day) when it is \code{TRUE}.
\item \code{beta_community}: The transmission rate for community contacts, used to compute
the per-timestep force of infection in community settings. A numeric scalar
when \code{time_varying_transmission_on} is \code{FALSE}, or a numeric vector of length
\code{simulation_time} (one value per simulated calendar day) when it is \code{TRUE}.
\item \code{time_varying_transmission_on}: Logical flag (default \code{FALSE}). When \code{FALSE},
each setting-specific beta must be a numeric scalar. When \code{TRUE}, each
setting-specific beta must instead be a numeric vector of length
\code{simulation_time}, giving that setting's beta for each simulated calendar day.
\item \code{dt}: TBD
\item \code{simulation_time}: TBD
\item \code{household_distribution_country}: TBD
\item \code{school_distribution_country}: TBD
\item \code{workplace_distribution_country}: TBD
\item \code{endemic_or_epidemic}: TBD
\item \code{duration_immune}: TBD
\item \code{prob_inf_external}: TBD
}

Rendering Parameters
\itemize{
\item \code{render_diagnostics}: FALSE
}

Setting-Specific ACH Parameters:
\itemize{
\item \code{setting_specific_ach_workplace}: boolean switch set to TRUE if setting-specific ACH parameterised in the workplace setting using \code{set_setting_specific_ach()}; default = FALSE
\item \code{setting_specific_ach_workplace_mean}: The mean of the  truncated normal distribution from which the setting-specific ACH of individual workplaces is drawn; default =4.8
\item \code{setting_specific_ach_workplace_sd}: The standard deviation of the truncated normal distribution from which the setting-specific ACH of individual workplaces is drawn; default =1.5
\item \code{setting_specific_ach_school}: boolean switch set to TRUE if setting-specific ACH parameterised in the school setting using \code{set_setting_specific_ach()}; default = FALSE
\item \code{setting_specific_ach_school_mean}: The mean of the truncated normal distribution from which the setting-specific ACH of individual schools is drawn; default = 4.0
\item \code{setting_specific_ach_school_sd}: The standard deviation of the truncated normal distribution from which the setting-specific ACH of individual schools is drawn; default = 1.2
\item \code{setting_specific_ach_leisure}: boolean switch set to TRUE if setting-specific ACH parameterised in the leisure setting using \code{set_setting_specific_ach()}; default = FALSE
\item \code{setting_specific_ach_leisure_mean}: The mean of the truncated normal distribution from which the setting-specific ACH of individual leisure locations is drawn; default = 3.0
\item \code{setting_specific_ach_leisure_sd}: The standard deviation of the truncated normal distribution from which the setting-specific ACH of individual leisure settings is drawn; default = 1.0
\item \code{setting_specific_ach_household}: boolean switch set to TRUE if setting-specific ACH parameterised in the household setting using \code{set_setting_specific_ach()}; default = FALSE
\item \code{setting_specific_ach_household_mean}: The mean of the truncated normal distribution from which the setting-specific ACH of individual households is drawn; default = 0.5
\item \code{setting_specific_ach_household_sd}: The standard deviation of the truncated normal distribution from which the setting-specific ACH of individual households is drawn; default = 0.2
}

Default (Uniform) ACH Parameters (used when \verb{setting_specific_ach_<setting>} is FALSE; if both are unset, \code{generate_setting_specific_ach()} errors):
\itemize{
\item \code{default_ach_workplace}: uniform ACH assigned to every workplace location when \code{setting_specific_ach_workplace} is FALSE. Set via \code{\link[=set_default_ach]{set_default_ach()}}. Default = NULL (must be set explicitly).
\item \code{default_ach_school}: uniform ACH assigned to every school location when \code{setting_specific_ach_school} is FALSE. Default = NULL.
\item \code{default_ach_leisure}: uniform ACH assigned to every leisure location when \code{setting_specific_ach_leisure} is FALSE. Default = NULL.
\item \code{default_ach_household}: uniform ACH assigned to every household location when \code{setting_specific_ach_household} is FALSE. Default = NULL.
}

Volume Per Person Parameters (used in the Wells-Riley calculation; units: m^3 per person):
\itemize{
\item \code{volume_per_person_workplace}: average air volume per person in a workplace; default = 27 (assumes ~10 m^2 floor area at 2.7 m height)
\item \code{volume_per_person_school}: average air volume per person in a school; default = 10 (assumes ~3.33 m^2 floor area at 3 m height)
\item \code{volume_per_person_leisure}: average air volume per person in a leisure setting; default = 8 (assumes ~2 m^2 floor area at 4 m height)
\item \code{volume_per_person_household}: average air volume per person in a household; default = 50 (assumes ~20 m^2 floor area at 2.5 m height)
}

Wells-Riley Parameters (used by \code{convert_ach_to_riskiness()} to derive per-location riskiness from ACH, and by \code{calculate_efficacy_from_ach()} to derive per-location intervention efficacy from ACH and the intervention delta):
\itemize{
\item \code{wells_riley_emission_rate}: rate at which an infectious individual emits airborne infectious units; units = FFU/hour; default = 27
\item \code{wells_riley_decay_rate}: natural decay rate of airborne pathogens (denoted k_D); units = 1/hour; default = 0.64
\item \code{wells_riley_infection_prob_per_ffu}: probability of infection per inhaled FFU (denoted r); default = 1.37e-2
\item \code{wells_riley_respiratory_rate_factor}: respiratory rate multiplied by tidal volume (denoted RR_tv); units = m^3/hour; default = 0.45
\item \code{wells_riley_time_in_room}: exposure window used inside the Wells-Riley calculation (denoted t); units = hours; default = 4
}

Intervention Parameters (populated internally by \code{set_intervention_ach()} and by \code{generate_intervention_switches()}; users do not normally set these directly. One block per scope \if{html}{\out{<s>}} in {joint, workplace, school, leisure, household}):
\itemize{
\item \verb{intervention_<s>_active}: boolean flag set to TRUE when an intervention has been installed in scope \if{html}{\out{<s>}}. Default = FALSE
\item \verb{intervention_<s>_list}: list of intervention objects (each as returned by \code{make_intervention()}) deployed in scope \if{html}{\out{<s>}}. Currently single-intervention only — list always has length 1 when active. Default = NULL
\item \verb{intervention_<s>_coverage}: fraction of total setting size to cover (numeric in \verb{[0, 1]}); inherited from the intervention object's \code{coverage} field. Default = NULL
\item \verb{intervention_<s>_coverage_target}: what the coverage fraction applies to. Either "individuals" or "square_footage". Default = NULL
\item \verb{intervention_<s>_coverage_type}: how locations are selected for coverage. Either "random" (uniform sampling) or "targeted_riskiness" (locations ranked in decreasing order of riskiness). Default = NULL
\item \verb{intervention_<s>_timestep}: first simulation timestep at which the intervention's efficacy is applied in the FOI calculation. Default = NULL
\item \verb{intervention_<setting>_covered} (per-setting scopes only — workplace/school/leisure/household): 0/1 vector of length equal to the number of locations in the setting, populated by the dispatcher to mark which locations received the intervention. Default = NULL
}

Setting-Specific Room Size Per Individual Parameters:
\itemize{
\item \code{size_per_individual_workplace}: The volume or surface area for each individual in the workplace setting type; default = 1 (in which case "square_footage" coverage_target gives same results as "individuals" coverage_target)
\item \code{size_per_individual_school}: The volume or surface area for each individual in the school setting type; default = 1 (in which case "square_footage" coverage_target gives same results as "individuals" coverage_target)
\item \code{size_per_individual_leisure}: The volume or surface area for each individual in the leisure setting type; default = 1 (in which case "square_footage" coverage_target gives same results as "individuals" coverage_target)
\item \code{size_per_individual_household}: The volume or surface area for each individual in the household setting type; default = 1 (in which case "square_footage" coverage_target gives same results as "individuals" coverage_target)
}

Hospitalizations and Deaths
\itemize{
\item \code{prob_hosp_child}: Probability that an infected child is hospitalized
\item \code{prob_hosp_adult}: Probability that an infected adult is hospitalized
\item \code{prob_hosp_elderly}: Probability that an infected elderly individual is hospitalized
\item \code{prob_death_hosp_child}: Probability of death for a hospitalized child, conditional on hospitalization
\item \code{prob_death_hosp_adult}: Probability of death for a hospitalized adult, conditional on hospitalization
\item \code{prob_death_hosp_elderly}: Probability of death for a hospitalized elderly individual, conditional on hospitalization
\item \code{duration_hospitalized}: Average duration (in days) of a hospitalization
}}

\item{archetype}{A text string indicating the pathogen archetype parameter set to load (default = "none", current options are flu, sars_cov_2, and measles)}
}
\description{
This function creates a named list of model parameters which are to be used
in the model. For example, the output of \code{\link[=get_parameters]{get_parameters()}} provides input to
functions such as \code{\link[=create_variables]{create_variables()}} and \code{\link[=create_events]{create_events()}}.
}
\concept{parameters}
