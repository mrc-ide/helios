% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_varying_transmission_calibration.R
\name{generate_time_varying_beta_vectors}
\alias{generate_time_varying_beta_vectors}
\title{Expand a transmission_fraction split and a beta_community vector into
the five setting-specific beta vectors Helios needs}
\usage{
generate_time_varying_beta_vectors(
  transmission_fraction,
  beta_community_vector
)
}
\arguments{
\item{transmission_fraction}{Named numeric vector with names \code{household},
\code{workplace}, \code{leisure}, \code{community}, summing to 1.}

\item{beta_community_vector}{Numeric vector, the time-varying community beta
(one value per simulated day). Typically the \code{beta_community} column from
the table returned by \code{\link[=get_beta_from_R0_finalsize]{get_beta_from_R0_finalsize()}}.}
}
\value{
A named list with five numeric vectors, each the same length as
\code{beta_community_vector}: \code{beta_household}, \code{beta_workplace}, \code{beta_school},
\code{beta_leisure}, \code{beta_community}. \code{beta_school} is always identical to
\code{beta_workplace}. Ready to pass directly into
\code{get_parameters(overrides = ...)} alongside
\code{time_varying_transmission_on = TRUE}.
}
\description{
Given a time-varying beta_community vector and a fixed transmission fraction
split, this function derives the corresponding time-varying beta values for
all settings. Setting-specific betas are scaled relative to beta_community
using the transmission fraction ratios, producing five vectors ready to pass
into \code{get_parameters()}.
}
